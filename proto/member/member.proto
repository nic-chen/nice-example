syntax = "proto3";

service Member {
    rpc Info (Request) returns (Response);
}

message Request {
    int64 id = 1;
}

message Response {
    int64 id = 1;
    string nickname = 2;
    string avatar = 3;
}